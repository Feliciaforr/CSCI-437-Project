This folder is used to store:
- Local SQLite database file (data.db)
- CSV backups or test data files
- Any downloaded historical stock data from Yahoo API

Note: data.db will be version-controlled if needed.